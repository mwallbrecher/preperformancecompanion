name: preperformancecompanion
description: "PrePerformance Companion — UX Research Prototype"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  sensors_plus: ^7.0.0
  speech_to_text: ^7.3.0
  shared_preferences: ^2.5.5
  path_provider: ^2.1.5
  flutter_animate: ^4.5.2
  csv: ^6.0.0
  share_plus: ^10.0.0
  file_picker: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
